/**
 * This is a file that will get removed once I start writting actual files for this project
 */

 function helloWorld(one: number, two: number): void {
     console.log(`Hello World! ${one * two}`)
 }