{
  "name": "portfoliowebsite",
  "version": "1.0.0",
  "description": "My personal portfolio website.",
  "main": "index.js",
  "scripts": {
    "transpile": "tsc -p tsconfig.generator.json && tsc -p tsconfig.site.json",
    "render": "npm run transpile && node ./_generator/generator/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Austin Scott",
  "license": "MIT",
  "dependencies": {
    "@types/fs-extra": "^8.0.1",
    "@types/node": "^13.1.6",
    "@types/pug": "^2.0.4",
    "@types/showdown": "^1.9.3",
    "fs-extra": "^8.1.0",
    "github-api": "^3.3.0",
    "pug": "^3.0.1",
    "showdown": "^1.9.1"
  }
}
